[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Single-page app: mọi route đều trả về index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
